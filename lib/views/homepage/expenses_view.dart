import 'package:flutter/material.dart';

class Expensesview extends StatelessWidget {
  const Expensesview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Here expenses will be displlayed"),
    );
  }
}
