import 'package:flutter/material.dart';

class IncomesView extends StatelessWidget {
  const IncomesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Here income will be displlayed"),
    );
  }
}
